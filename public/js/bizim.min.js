$('#yetkililer,#isletmeler, #ogrenciler').DataTable({
    responsive: true,
});

function yetkiliSilme(id) {
    event.preventDefault();
    Swal.fire({
        title: 'Kullanıcıyı silmek istediğinizden emin misiniz?',
        text: "Silme işlemi geri alınamayabilir!",
        icon: 'warning',
        showCancelButton: true,
        confirmButtonColor: '#3085d6',
        cancelButtonText: "İptal!",
        cancelButtonColor: '#d33',
        confirmButtonText: 'Evet, silinsin!',
    }).then((result) => {
        if (result.isConfirmed) {
            $('#yetkiliSilme-' + id).submit();
            Swal.fire(
                'Silindi!',
                'Üye silindi.',
            )
        }
    })
}

function isletmeSilme(id) {
    event.preventDefault();
    Swal.fire({
        title: 'İşletmeyi silmek istediğinizden emin misiniz?',
        text: "Silme işlemi geri alınamayabilir!",
        icon: 'warning',
        showCancelButton: true,
        confirmButtonColor: '#3085d6',
        cancelButtonText: "İptal!",
        cancelButtonColor: '#d33',
        confirmButtonText: 'Evet, silinsin!',
    }).then((result) => {
        if (result.isConfirmed) {
            $('#isletme-' + id).submit();
            Swal.fire(
                'Silindi!',
                'İşletme silindi.',
            )
        }
    })
}

function ogrenciSilme(id) {
    event.preventDefault();
    Swal.fire({
        title: 'İşletmeyi silmek istediğinizden emin misiniz?',
        text: "Silme işlemi geri alınamayabilir!",
        icon: 'warning',
        showCancelButton: true,
        confirmButtonColor: '#3085d6',
        cancelButtonText: "İptal!",
        cancelButtonColor: '#d33',
        confirmButtonText: 'Evet, silinsin!',
    }).then((result) => {
        if (result.isConfirmed) {
            $('#isletme-' + id).submit();
            Swal.fire(
                'Silindi!',
                'Üye silindi.',
            )
        }
    })
}

function dokumanTuruSilme(id) {
    event.preventDefault();
    Swal.fire({
        title: 'Döküman türünü silmek istediğinizden emin misiniz?',
        text: "Silme işlemi geri alınamayabilir!",
        icon: 'warning',
        showCancelButton: true,
        confirmButtonColor: '#3085d6',
        cancelButtonText: "İptal!",
        cancelButtonColor: '#d33',
        confirmButtonText: 'Evet, silinsin!',
    }).then((result) => {
        if (result.isConfirmed) {
            $('#dokumanTuru-' + id).submit();
            Swal.fire(
                'Silindi!',
                'Döküman silindi.',
            )
        }
    })
}

function isletmeBasvuru(id) {
    event.preventDefault();
    Swal.fire({
        title: 'İşletmeye başvuru yapmak üzeresiniz.',
        text: "İşletmeye başvuru yaptıktan sonra başka bir işletmeye onay süreciniz tamamlanana kadar başvuru yapamayabilirsiniz.",
        icon: 'warning',
        showCancelButton: true,
        confirmButtonColor: '#3085d6',
        cancelButtonText: "İptal!",
        cancelButtonColor: '#d33',
        confirmButtonText: 'Evet, başvur!',
    }).then((result) => {
        if (result.isConfirmed) {
            /*
                        let internship_type = $("#staj-turu").val(); // $( "#staj-turu option:selected" ).text(); // document.getElementById("staj-turu").value;//
                        let internship_start_date = document.getElementById("internship_start_date").value;
                        let internship_end_date = document.getElementById("internship_end_date").value;
                        $('#hidden_internship_type-' + id).val(internship_type);
                        $('#hidden_internship_start_date-' + id).val(internship_start_date);
                        $('#hidden_internship_end_date-' + id).val(internship_end_date);
            */
            $('#isletme-' + id).submit();
        }
    })
}





